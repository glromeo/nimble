{
  "name": "sample",
  "version": "1.0.0",
  "license": "MIT",
  "config": {
    "baseUrl": "http://localhost:3000/sample"
  },
  "scripts": {
    "build": "nimble build --release",
    "start": "nimble start --watch --debug"
  },
  "dependencies": {
    "@nimble/toolkit": "^1.0.0"
  },
  "devDependencies": {
    "@nimble/scripts": "^1.0.0",
    "@nimble/stories": "^1.0.0"
  }
}

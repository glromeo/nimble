{
  "name": "{{it.name}}",
  "version": "1.0.0",
  "license": "MIT",
  "config": {
    "baseUrl": "http://localhost:3000/{{it.name}}"
  },
  "scripts": {
    "build": "nimble build --release",
    "start": "nimble start --watch"
  },
  "dependencies": {
    "@nimble/toolkit": "^1.0.0"
  },
  "devDependencies": {
    "@nimble/scripts": "^1.0.0"
  }
}
